name: TEMP Apply Stage2 Metaphor 5
on:
  push:
    branches: [main]
    paths: ['.github/workflows/temp-apply-stage2-metaphor-5.yml']
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply approved edits
        shell: bash
        run: |
          python - <<'PY'
          import json, glob
          from pathlib import Path
          ap=Path('sourcecards-targeted-review-7passes/cycle2-pass12-unnecessary-metaphor/audit.json')
          audit=json.loads(ap.read_text(encoding='utf-8'))
          cs=audit['후보']
          if len(cs)!=5: raise SystemExit(f'expected 5 got {len(cs)}')
          staged={}; ids=set()
          for c in cs:
              p=Path(c['파일'])
              text=staged.get(p,p.read_text(encoding='utf-8'))
              old,new=c['수정전전체'],c['수정후전체']
              n=text.count(old)
              if n!=1: raise SystemExit(f"{c['구절ID']}: match={n}")
              text=text.replace(old,new,1)
              json.loads(text)
              staged[p]=text; ids.add(c['구절ID'])
          if len(ids)!=5: raise SystemExit(len(ids))
          for p,text in staged.items(): p.write_text(text,encoding='utf-8')
          for c in cs:
              c['승인상태']='승인완료'; c['본문반영']=True
          audit['승인상태']='승인완료'; audit['본문반영수']=5; audit['반영완료']=True
          ap.write_text(json.dumps(audit,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          files=sorted(glob.glob('sourcecards-style-polish/[0-9][0-9]-*.json'))
          if len(files)!=20: raise SystemExit(len(files))
          total=sum(len(json.loads(Path(f).read_text(encoding='utf-8'))['장']) for f in files)
          if total!=498: raise SystemExit(total)
          print('applied',len(cs),'total',total)
          PY
      - name: Commit
        shell: bash
        run: |
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add sourcecards-style-polish/ sourcecards-targeted-review-7passes/cycle2-pass12-unnecessary-metaphor/audit.json
          git commit -m '사료카드 2단계 불필요한 비유 5건 실제 반영'
          git push origin main
