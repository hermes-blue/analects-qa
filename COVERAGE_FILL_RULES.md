# 논어 쟁점 보완 패스 룰

## 목적
기존 `track/issues/쟁점후보/`의 강한 학설 대립형과 `쟁점확장/`의 선택적 확장형을 그대로 보존하고, 두 층 어디에도 최종 쟁점이 없는 장만 다시 채운다.

## 원칙
1. 기준 장 배열은 `main/data`를 따른다.
2. `existing_strong_issue:true`이거나 `쟁점확장`에 `selected`가 있으면 보완 대상에서 제외한다.
3. 그 밖의 장은 원칙적으로 1개씩 만든다. 이번 패스에서는 ‘빈 배열을 선호’하지 않는다.
4. 유형은 본문 논쟁형·장간 긴장형·문장 안 긴장형·현실 선택형·역사/인물 평가형·개념 범위형·오해 교정형·시대/제도 맥락형·적용 한계형 가운데 가장 가까운 것을 쓰되, 완벽한 분류보다 실제로 흥미롭고 본문에 근거한 질문을 우선한다.
5. 문답 카드와 주제가 일부 겹쳐도 된다. 다만 단순히 기존 답을 질문형으로 바꾸는 데 그치지 않고, 선택·오해·범위·긴장·평가 중 하나의 생각거리를 만든다.
6. 억지 학자 이름, 근거 없는 역사 사실, 원문에 없는 심리 추측은 금지한다.
7. 반복 구절도 해당 장에 카드가 없으면 전승 반복, 문맥 차이, 적용 범위 등 그 장에서 살릴 수 있는 축을 찾는다.
8. 보완 결과는 `쟁점보완/` 아래에 편별 JSON으로 저장한다.

## 보완 JSON
각 항목은 `chapter`, `id`, `type`, `basis`, `headline`, `answer_frame`, `selected:true`를 가진다. 이 보완층은 기존 두 층을 대체하지 않고 합쳐서 사용한다.